Working Title: Inverse Mapper
=========================

Given a string H and a string V, an integer k and an error rate \epsilon,
find substrings of V with a size k that have no match in H with an error
rate smaller than \epsilon.
